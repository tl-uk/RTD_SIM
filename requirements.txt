# Phase 1 uses stdlib only (no external deps)
# Keep this empty or add future deps in Phase 3/4:
# streamlit
# plotly
# folium
# osmnx
# paho-mqtt
# websockets

# RTD_SIM Requirements
# Phase 1-3 Dependencies

# Core dependencies (Phase 1-2)
# Already installed in your environment
osmnx>=1.9.0
networkx>=3.0
pandas>=2.0
numpy>=1.24
streamlit>=1.40.0
pydeck>=0.9.0
plotly>=5.0
requests>=2.31.0

# Phase 3: Story Framework
pyyaml>=6.0

# Future phases (commented out for now)
# paho-mqtt  # Phase 5: MQTT integration
# websockets  # Phase 5: Real-time streaming
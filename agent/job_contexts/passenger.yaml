# agent/job_contexts/passenger.yaml
#
# Passenger job contexts — SHORT TRIPS and PERSONAL ERRANDS only.
#
# REORGANISATION NOTE (v2):
#   morning_commute, commute_flexible, and shopping_trip have been MOVED to
#   transit_passenger.yaml, which provides more complete definitions with
#   correct vehicle_type, preferred_modes, and explicit priority fields.
#
#   Keeping duplicate keys here caused non-deterministic parser behaviour and
#   produced priority=commercial for commute jobs (bug confirmed 2026-03-21).
#   The fix: transit_passenger.yaml is now the single authoritative source for
#   all passenger/commute/shopping trip job contexts.
#
# This file is intentionally empty — retained for backwards-compatible import.
# See transit_passenger.yaml for all passenger job contexts.
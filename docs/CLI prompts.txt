# Workflow
git pull --rebase origin main

then after

git add -A && git commit -m "... comments ..." && git push https://github.com/tl-uk/RTD_SIM.git main 

# Terminal tracking for ollama BDI-agent user/job stories generation
tail -f /Users/theolim/AppDev/RTD_SIM/logs/simulation_*.log | grep -E "(LLM|CPG|OLMo|olmo|extraction|fallback|plan|backend)"

# Report project structure
find /Users/theo/Dev/Python/RTD_SIM -type f -name "*.py" -o -name "*.yaml" | \
  grep -v __pycache__ | grep -v .git | sort > project_structure_current.txt && \
  echo "Done: $(wc -l < project_structure_current.txt) files"

# list all files in cache
ls -lah ~/.rtd_sim_cache/transport

# preview all matching cached rail/tram GraphML files
find ~/.rtd_sim_cache/transport \
  \( -name "*_rail.graphml" -o -name "*_tram*.graphml" \) \
  -type f

# count how many found
find ~/.rtd_sim_cache/transport \
  \( -name "*_rail.graphml" -o -name "*_tram*.graphml" \) \
  -type f | wc -l

# delete safely
find ~/.rtd_sim_cache/transport \
  \( -name "*_rail.graphml" -o -name "*_tram*.graphml" \) \
  -type f -delete

# find files and delete (f -delete)
find ~/.rtd_sim_cache/transport \
  \( -name "*_rail.graphml" -o -name "*_tram*.graphml" -o -name "*_ferry*.graphml" \) \
  -type f -delete

# find cache files
find ~/.rtd_sim_cache -type f

# list cache files
ls -lah ~/.rtd_sim_cache/osm_graphs

# delete and force rebuild
rm -rf ~/.rtd_sim_cache

# clear OSM cache
rm -rf ~/.rtd_sim_cache/osm_graphs/
rm ~/.rtd_sim_cache/transport/ferry_shim_ferry.graphml   
rm ~/.rtd_sim_cache/transport/default_ferry.graphml      
rm ~/.cache/osmnx/*.json                                 



